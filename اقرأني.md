# أكاديمية فقه المسلم — دليل الاستخدام

## 1) شكل البيانات دلوقتي (بعد التحديث)
الموقع بقى منظّم على أساس "مواد" (subjects) داخل كل مستوى، مش فيديوهات مباشرة.
كل المحتوى بييجي من ملف واحد بس:
```
js/data.js
```

كل مستوى فيه مصفوفة `subjects`، وكل مادة فيها:
- `name`: اسم المادة (القرآن، التجويد، الفقه...)
- `teacher`: اسم الشيخ (أو "قريبًا" لو لسه ماتحددش)
- `teacherNote`: وصف قصير اختياري
- `goal`: هدف المادة في المستوى ده
- `topics`: قائمة الموضوعات
- `reference`: (اختياري) اسم المتن أو الكتاب

المستوى الأول اتحط كامل بالمنهج اللي بعتهولي. المستويات 2 و3 و4 لسه فاضية
("قريبًا") — تحطلهم نفس شكل المستوى الأول أول ما تجهزلي المنهج.

## 2) إزاي تضيف فيديو حقيقي لموضوع معيّن
لسه مفيش فيديوهات متربطة، بس البنية التحتية جاهزة (صفحة `lesson.html` موجودة).
لما يكون عندك روابط فعلية، قولّي وهربطها بكل مادة.

## 3) التصميم الجديد
- الشعار بقى حرف "ف" في دايرة ذهبية
- الاسم اتغيّر لـ "أكاديمية فقه المسلم" بخط "Aref Ruqaa" العربي الفخم
- قايمة علوية بـ 3 عناصر: الرئيسية / المستويات (بها قائمة منسدلة) / عن الأكاديمية
- تأثيرات توهج ذهبي على العناوين والكروت عند التمرير عليها

## 4) الرفع على GitHub (زي ما عملنا)
بعد أي تعديل في الملفات، ارفعه بنفس الطريقة:
```bash
git add .
git commit -m "وصف التعديل"
git push
```
GitHub Pages هيحدّث الموقع تلقائي خلال دقيقة أو اتنين، من غير أي خطوة إضافية.

## 5) حاجات جاية
- منهج المستويات 2، 3، 4
- ربط فيديوهات حقيقية بكل موضوع
- تسجيل دخول وحفظ تقدم دائم (Supabase)
